playwright==1.52.0
websockets>=13.0,<14.0
# Upper bounds added to keep pip's resolver from hitting `resolution-too-deep`
# in the playwright/python:v1.52.0-noble base image. Without these, the
# Docker build fails after ~4 minutes of pip backtracking. Confirmed on
# both local docker builds (2026-05-06) and CI (deploy.yml run 25490632880
# on 2026-05-07).
boto3>=1.35.0,<2.0
aioboto3>=13.0.0,<14.0
httpx>=0.27.0,<1.0
