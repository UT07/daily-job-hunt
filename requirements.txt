# Job Automation Pipeline - Dependencies

# Core
pyyaml>=6.0
requests>=2.31.0

# Excel tracker
openpyxl>=3.1.2

# Optional: for data analysis of tracker
pandas>=2.0.0

# Browser-based scrapers (LinkedIn, Indeed, IrishJobs)
playwright>=1.40.0
playwright-stealth>=1.0.6

# Async support
nest-asyncio>=1.5.8

# S3 upload for artifacts
boto3>=1.34.0

# Google Drive API (upload PDFs for shareable links)
google-api-python-client>=2.100.0
google-auth>=2.25.0

# Supabase (Postgres + Auth + RLS for multi-tenant SaaS)
supabase>=2.0.0

# Optional: Anthropic SDK (only needed if using Claude as paid fallback)
# anthropic>=0.39.0
