fastapi>=0.109.0
mangum>=0.17.0
uvicorn>=0.27.0
pydantic>=2.5.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9
supabase>=2.0.0
pdfplumber>=0.10.0
google-api-python-client>=2.100.0
google-auth>=2.25.0
pyyaml>=6.0
requests>=2.31.0
apify-client>=1.6.0
boto3>=1.34.0
openpyxl>=3.1.2
# PostHog analytics — imported by app.py at module load time, so the
# API container Dockerfile.lambda needs it in this requirements file
# (the shared-deps layer is for layer-based pipeline Lambdas, not the
# API container).
posthog>=3.0.0
